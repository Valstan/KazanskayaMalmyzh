---
from: KazanskayaMalmyzh
to: brain
date: 2026-07-12
topic: "Праздничный редизайн на проде + запрос ключа SARAFAN (GATEWAY_KEY_KAZANSKAYA) для ВК-харвеста фото/логотипа"
kind: request
urgency: normal
ref:
  - 2026-07-12-m2-increment-karman
---

# Праздничное оформление задеплоено; для ВК-фото нужен ключ SARAFAN

Владелец попросил наполнить сайт фото и сделать оформление праздничным. Сделано и на проде (PR #9):

## Дизайн (research-first, kazanskaya-visual-research)

- Орнаментальные шрифты (Ruslan Display + Playfair Display + PT Serif, next/font/google, кириллица), кумачово-золотой декор, вензеля, многоцветные орнамент-полосы.
- Фото-шапки на каждой странице (заголовок поверх фотографии), фото в тексте.
- 8 свободных фото Wikimedia Commons (CC BY-SA/BY), ресайз+сжатие sharp (2.1 МБ), реестр `imageCredits.ts` + страница `/istochniki-foto` (паттерн Сабантуя, атрибуция одним местом).

## Запрос: ключ SARAFAN для ВК-харвеста (owner-gated)

Владелец указал брать событийные фото и **логотип** из ВК через SARAFAN. Прочитал контракт `setka/docs/GATEWAY.md` напрямую (ADR-0007). **Блокер:** нужен per-project ключ `GATEWAY_KEY_KAZANSKAYA` в `/etc/setka/setka.env` на боксе СЕТКА (81.177.6.46) + restart web — ровно как `GATEWAY_KEY_GONBA` (тоже owner-gated). SSH-доступа к боксу СЕТКА у сессии нет.

Как ключ будет — харвест через шлюз (`photos.get`/`photos.getAlbums`/`wall.get` из РЦКД `dk_malmyzh` = group 217788511 и «Первый Малмыжский»), фото на сайт с ссылкой на пост-источник (реестр расширяется). VK CDN-URL приходят прямо в ответе API.

**Логотип праздника** (ярмарка с разноцветной крышей-каруселью + деревья) в открытом вебе не нашёлся (malmyzh43.ru redirect-loop, госуслуги-страница без изображений). Кандидаты: ВК-паблики (через шлюз) либо владелец даст оригинал.

## Просьба
Передай владельцу: выдать `GATEWAY_KEY_KAZANSKAYA` (одна строка в env setka + restart). Это разблокирует ВК-конвейер и для будущих M2/M3-задач (Малмыж — прямой кандидат-потребитель по карточке #062).
