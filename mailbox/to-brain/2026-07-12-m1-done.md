---
from: KazanskayaMalmyzh
to: brain
date: 2026-07-12
topic: "M1 DONE: https://казанская.вмалмыже.рф/ отвечает 200 с контент-маркером; каркас на проде за одну сессию"
kind: report
urgency: high
ref:
  - 2026-07-12-date-confirmed-fasttrack-m1-m2
---

# M1 закрыт в день старта (DoD достигнут)

- **PR #1** (scaffold+CI) и **PR #2** (фикс `public/` в деплое) смержены через зелёные гейты.
- **Deploy-ключ #001:** владелец делегировал сессии; сгенерирован изолированный `id_ed25519_kazanskaya` (приватная часть — secrets.SSH_PRIVATE_KEY репо + dev-машина владельца), pubkey — в authorized_keys бокса.
- **Бокс подготовлен сессией** (SSH-доступ с dev-машины работал, sudo NOPASSWD): БД `kazanskaya`+роль, `/etc/kazanskaya/kazanskaya.env` (0600), юнит kazanskaya (MemoryMax=768M, :3001), nginx-vhost казанская → :3001.
- **Деплой зелёный:** standalone из CI, releases/<sha>, смоук #011 — `/` 200 + маркер «Ярмарка Казанская», `/admin` 200.
- **TLS выпущен** (certbot --nginx, LE): `https://казанская.вмалмыже.рф/` = **200 + маркер** (проверено с бокса; с dev-машины 000 — G147 DPI, судим по ACME-успеху и DNS 8.8.8.8).

## Отклонение от плана — к сведению

G8 («с dev-машин бокс режется») не подтвердился для SSH: dev-машина владельца ходит на бокс по ключу sabantuy свободно. Возможно, G8 касался только HTTP/DPI (G147). Уточнить формулировку гочи.

## Хвост для владельца

KARMAN-зеркало (ADR-0006): нужна комната `kazanskayamalmyzh` + rw-токен (owner-действие в KARMAN → /secrets). Зеркалировать: deploy-ключ, DATABASE_URL, PAYLOAD_SECRET.

## Далее

M2 (≤ 23.07): research-first дизайн по visual-research, страницы истории, программа-2026 (снять пакет документов района), карта, темы по годам, SEO #051, pageDecor R7.
