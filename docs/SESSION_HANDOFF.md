# SESSION_HANDOFF — sticky-note между сессиями (pool #003)

**Обновлено:** 2026-07-20 (сессия 5: скоморох-гид и сказочные помощники — на проде)

## TL;DR для следующей сессии

**M1 ✅, M2 ✅, M3-минимум ✅, редизайн ✅** — до праздника 5 дней (сб 25.07.2026). Сайт https://xn--80aaa0andu6a3j.xn--80adkdyec4j.xn--p1ai/ полностью в новой теме; главная дополнена серией скомороха-гида. **Следующий шаг:** `re-probe бокса с тремя жильцами` — единственный незакрытый технический пункт (письмо brain от 18.07, `recommend`, до 25.07). Дальше — только owner-хвосты и M4 (пост-обзор) после дня X.

## Состояние (что на проде)

- **Тема «Ярмарочный лубок»** (PR #29): палитра (`#D32F2F` primary под WCAG AA, `#FFB300` accent, кремовый фон), шрифты Yeseva One / Nunito Sans / PT Sans, контейнер 1200px, SVG-паттерн фона. Шапка sticky + **бургер-меню** на мобайле (`SiteNav`). Первый экран главной — «информация первой»: дата, тема, **живой обратный отсчёт** (`Countdown`, после старта → «Праздник идёт!», через сутки исчезает), кнопки «Программа» и «Как добраться»; на мобайле фото-полоса + светлая панель. Внутренние страницы — компактные фото-плашки.
- **/program**: таймлайн событий первым, официальная афиша — вторичной секцией, FAQ — `<details>`-аккордеон (видимый контент сохранён, FAQPage JSON-LD валиден). Live-режим дня X (±36 ч) на месте.
- **/map** (PR #30): схемы территории от оргкомитета в 2026 нет → площадки собираются **из афиши** (группировка Events по `location`) с расписанием на каждой: парк/сквер, центральные улицы → стадион, стадион, площадка в парке, РЦКД. Адресов нет сознательно (решение владельца). Появится схема — `planImage` глобала `festival-map` выведется над списком.
- **/years**: порядок годов починен (2019 стоял между 2012 и 2011 — ломал и сетку, и навигацию «предыдущий/следующий»; экспорт теперь сортирует). У годов без фото — орнаментальная заглушка с плашкой года.
- Аналитика (Метрика 110809380 + LiveInternet 8155), SEO-пакет #051, летопись /years с 13 страницами — без изменений с сессии 3.

## Сделано в сессии 4 (все PR смержены, деплои зелёные, смоук с бокса пройден)

- **PR #27** — фикс порядка годов + `design/brief-2026-07-19/` (бриф и два промпта для ChatGPT, 9 скриншотов прода).
- **PR #28** — `design/**` в `paths-ignore` деплоя + pioneer-письмо brain о петле #079.
- **PR #29** — редизайн «Ярмарочный лубок» по спецификации ChatGPT.
- **PR #30** — площадки на /map из афиши + `docs/owner-requests/…zapros-temy-shestviy.md` (готовый текст запроса в РЦКД/музей).

## Сделано в сессии 5 (PR смержен, деплой зелёный, смоук пройден)

- **PR #33** — серия из шести WebP-образов скомороха-гида (`web/public/decor/guides/`): приветствие, программа, история с царевной, ремёсла с детьми, карта и прошлые годы.
- Главная (`web/src/app/(frontend)/page.tsx` + `festive.css`) получила персонажа на hero, декоративные карточки разделов, реплики-подсказки, помощников и адаптивные mobile-правила.
- Визуально проверены desktop/mobile; `pnpm typecheck`, ESLint и `git diff --check` зелёные. Production workflow `29743119108` завершён успешно, встроенный smoke `/`, `/admin` и content marker пройден.

## Ноу-хау сессии

- **Скриншоты прода с dev-машины**: browser-pane залипает, headless `--screenshot` не пишет в `d:\` (права), окно уже ~500px не сжимается. Рабочая связка — SSH-туннель к :3001 бокса + **puppeteer-core** (ставится в scratchpad, использует системный Chrome) с честным `setViewport` и **request-interception на `mc.yandex.ru`/`counter.yadro.ru`** — без этого `networkidle2` висит на DPI. Скрипт — в scratchpad сессии.
- **Петля #079**: canonical — текстовая спека, не картинки (в трёх развёртках ChatGPT токены расходятся между собой). Просить WCAG-таблицу прямо в промпте — модель честно пересчитала и сама затемнила красный. Запрещать кириллицу на картинках.
- **Прод-БД локально не трогаем**: dev-режим Payload умеет пушить схему. Вёрстку, зависящую от данных, проверяем после деплоя на проде.
- ISR: первый запрос после деплоя отдаёт каркас, собранный в CI без БД (`revalidate 60`) — при смоуке страниц с данными опросить повторно.

## Следующая сессия

1. **Re-probe бокса с тремя жильцами** (`sabantuy`, `kazanskaya`, `rmz`) по рецепту #061 — до сб 25.07: RAM в покое и под нагрузкой на казанскую, RSS по юнитам, throttle/oom-kill `rmz` в journald. Avail под нагрузкой < ~200 МБ → предложить владельцу остановить `rmz` на день X. Результат — письмом в `mailbox/to-brain/`.
2. После дня X: M4 (пост-обзор, архив года-2026, свежие фото в галерею), отложенное из спеки редизайна (SVG-пиктограммы площадок, галерея историями).

## Блокеры (owner)

- **Темы шествий 2012, 2014, 2017–2018, 2020–2021** — готовый текст запроса лежит в [`docs/owner-requests/2026-07-19-zapros-temy-shestviy.md`](owner-requests/2026-07-19-zapros-temy-shestviy.md), владелец отправляет в РЦКД / краеведческий музей. Ответ вносим в `web/src/lib/years.ts`.
- Схема территории праздника — если оргкомитет всё же выдаст, загрузить в глобал `festival-map` (код готов).
