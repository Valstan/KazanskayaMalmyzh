# SESSION_HANDOFF — sticky-note между сессиями (pool #003)

**Обновлено:** 2026-07-17 (аналитика PR #22 + SEO-ревизия #051 PR #24)

## TL;DR для следующей сессии

**M1 ✅, M2 ✅ (код целиком), M3-минимум ✅** — за 8 дней до праздника (сб 25.07.2026). Сайт https://казанская.вмалмыже.рф/ полный: афиша-2026 в Events и постером на /program, live-режим дня X, летопись /years с 13 страницами годов и фото, **аналитика (Метрика + LiveInternet) и SEO-ревизия #051 — на проде** (сессия 3, 17.07). **Следующий шаг:** остались только owner-хвосты (карта-схема территории, темы шествий 2012–2021 через РЦКД/музей); по коду — опционально M3-расширения (лента «Смотреть подряд», пост-обзор) после дня X.

## Состояние (что на проде)

- **Афиша-2026** (официальная, пост РЦКД `-217788511_5327`): 8 событий 25.07 в Payload Events (published, REST), постер на /program с атрибуцией; /program отдаёт программу.
- **Live-режим /program** (M3-минимум, PR #19): баннер «Сейчас идёт / Далее» + бейджи live/next/past, тик раз в минуту, включается в окне ±36 ч вокруг дня X (до праздника страница обычная). События без endDate считаются идущими 1 час.
- **Летопись /years** (PR #20): карточная сетка с мини-фото + 13 статических страниц `/years/<год>` (2010–2016, 2019, 2022–2026) — реестр `web/src/lib/years.ts` (абзацы/факты/фото/источники; заместил YEAR_THEMES из site.ts). 14 фото в `/public/years` (sharp ≤1200px, длинный кэш `/years/y*`). Годы без страниц (2012/2014/2017-18/2020-21) → 404, карточки без ссылки.
- **/llms.txt**, **Метрика-заготовка** (компонент `Metrika`, включается env `NEXT_PUBLIC_YM_ID`) — PR #17.
- **Capacity-probe #061 пройден** (16.07): 493/1536 МБ, оба юнита active — тесноты нет.

## Сделано в сессии 2 (все PR смержены, деплой+смоук зелёные)

- PR #17 — темы карнавала 2022–2024 (харвест SARAFAN), /llms.txt, Метрика-стаб; афиша-2026 → Events (данные, вне диффа).
- PR #18 — афиша-постер на /program (адаптация pageDecor R7).
- PR #19 — live-режим программы (ядро ScheduleList Сабантуя, ужато).
- PR #20 — летопись /years: повторный харвест 3 пабликов (~45k постов), ретро-фотоотчёты 2010–2019 у «Первого Малмыжского».

## Ноу-хау сессии (детали — в memory Claude)

- `vk-via-sarafan`: ключ шлюза из env setka; id групп; **пост-ids всех фотоотчётов по годам** (добор фото без повторного прогона 40k постов).
- `karman-secrets-decrypt`: PAYLOAD_ADMIN-креды без rw-токена — расшифровка на боксе GONBA (AES-GCM, AAD `secrets:11:<key>`).
- `box-access-and-checks`: на этой dev-машине ssh-алиас `sabantuy` (порт **49338** на 17.07 — myjino ротирует, сверяйся с `~/.ssh/config`), ключи `*_deploy`; прод-чек через `curl -H 'X-Forwarded-Proto: https' -H 'Host: <punycode>' http://127.0.0.1/` (443 у Джино). NB: `NEXT_PUBLIC_*` инлайнится в CI-сборке из GH-переменных, env бокса для них не нужен.

## Следующая сессия

1. По остатку — M3-расширения (лента, пост-обзор через SARAFAN) и/или сезонный M4 после 25.07 (post-mortem, архив года).

## SEO-ревизия #051 (готово, 2026-07-17, PR #24)

- `web/src/lib/seo.ts` — общий Festival JSON-LD (даты со временем, афиша+логотип, offers-бесплатно, organizer); главная и /program используют его.
- /program: subEvent из опубликованных Payload-событий + FAQPage-разметка и видимый блок «Частые вопросы».
- Устаревшие «афиша появится» переписаны (главная → ссылка на /program).

## Аналитика (готово, 2026-07-17, PR #22)

- **Яндекс.Метрика** — счётчик **110809380** (аккаунт ekipagh), подключён через GH-переменную `NEXT_PUBLIC_YM_ID` (инлайн в CI-сборке, env бокса не нужен). Вебвизор вкл., адрес-фильтр = домен. На проде работает.
- **LiveInternet** — публичный счётчик id **8155** (тип t11.6, 88×31, три числа) в подвале всех страниц. На проде работает.

## Блокеры (owner)

- Карта-схема территории праздника → FestivalMap.
- Темы шествий 2012, 2014, 2017–2018, 2020–2021 (+фото) — запрос в РЦКД / краеведческий музей.
